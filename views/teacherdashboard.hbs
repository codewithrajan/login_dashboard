<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Dashboard</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
        }

        .dashboard-container {
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            margin: 20px;
        }

        h2 {
            color: #333;
            margin-bottom: 10px;
        }

        .user-info {
            margin-bottom: 20px;
        }

        .logout-btn {
            background-color: #dc3545;
            color: #fff;
            padding: 10px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-weight: bold;
        }

        .logout-btn:hover {
            background-color: #c82333;
        }

        /* Responsive styles */
        @media only screen and (max-width: 600px) {
            .dashboard-container {
                margin: 10px;
            }
        }
    </style>
</head>

<body>

    <div class="dashboard-container">
        <div class="user-info">
            <h2>Welcome <span style="color:red">{{user}}</span>!,And your are <span style="color:green;">{{role}}</span></h2>
        </div>
        <div>
           Content as per your choice
        </div>
        <button class="logout-btn" onclick="logout()">Logout</button>
    </div>

    <script>
        function logout() {
            // Implement logout functionality here
            alert("Logout successful!");
            window.location.href = '/login';

            // Redirect to the login page or perform other actions as needed
        }
    </script>

</body>

</html>